async function main(){
    // console.log('Hi I will compile and deploy contract here');
    // compile them in code
    
}

main().then(() => process.exit(0)).catch((error) => {
    console.error(error);
    process.exit(1);
});